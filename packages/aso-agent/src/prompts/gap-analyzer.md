we've just completed implementation for the following objective:
{{original_objectives}}

git log since session started:
{{git_log}}

review the changes, session history, git logs, etc. and identify gaps in our changes around:
- incomplete implementations, e.g. no end-to-end integrations between implementations in two areas
- security concerns - especially around authorizations, gating features, permissions
- testing gaps, lack of coverage, cases, etc.
- lack of error handling
- user experience gaps - missing buttons, orphaned pages, etc.
- edge cases
- missing or inadequate documentation

Use the aso-agent skills to do a gap-report command to report these
